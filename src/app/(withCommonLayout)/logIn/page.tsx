import LoginPage from '@/components/auth/LoginPage';

import React from 'react';

const page = async () => {

  
    return (
        <div>
            <LoginPage/> 
        </div>
    );
};

export default page;